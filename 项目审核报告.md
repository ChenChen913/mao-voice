# AI 语音输入法（mao-voice）项目审核报告

| 项 | 内容 |
| --- | --- |
| 审核日期 | 2026-08-03 |
| 审核范围 | 代码、文档、测试、GitHub 仓库、对照 PRD/SPEC 的功能完成度 |
| 数据依据 | 实际代码 3500 行/14 模块、8 轮 OCR 代码审查记录、PRD/SPEC/部署文档、Git 历史（4 次提交） |

---

## 1. 项目概览

**定位**：Windows 桌面 AI 语音输入法——按一下右 Alt 开始录音，再按一下结束，文字经"本地语音识别（faster-whisper）+ 大模型保守纠错（DeepSeek）"后自动注入当前输入框。

| 维度 | 现状 |
| --- | --- |
| 代码规模 | 14 个 Python 模块，约 3,500 行（核心运行时代码，不含文档/模型） |
| 运行形态 | 后台常驻 + 迷你波形胶囊悬浮窗（150×52，不抢焦点） |
| GitHub | `ChenChen913/mao-voice`（公开，main 分支，6 次提交，首页 README 正常渲染） |
| 文档 | README + 部署文档 + PRD + SPEC + 调研文档 + OCR 审查报告，共 6 份 |
| 开发方式 | 人机协作：需求拷打 → PRD/SPEC → MVP 实现 → 多轮 Agent 迭代 → 专业代码审查 |

**已完成的关键里程碑**
- P0 功能全部落地，可现场演示完整闭环（按住 → 识别 → 纠错 → 注入）
- 悬浮窗经历 3 轮用户反馈迭代（大窗 → 迷你 → 150×52 音频波胶囊，并修复"二次录音错位"根因）
- 修复两个真实运行根因：模型下载（HuggingFace 被墙 → ModelScope 本地化）、CUDA 库缺失（→ GPU 0.18s/次）
- 通过阿里 open-code-review 8 轮扫描 + 3 轮 Agent 修复 + 编排层精准修复：**critical/high/security 清零**

---

## 2. 完成度评估（对照 PRD 功能矩阵）

### 2.1 P0 核心功能 —— 全部完成 ✅

| 编号 | 需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| F1 | 全局说话触发 | ✅ | 已从"按住说话"演进为 **toggle（按一下开始/再按一下结束）**，含 200ms 防抖、AltGr 兼容 |
| F2 | 本地录音 | ✅ | sounddevice 16kHz 单声道，含 RMS 电平回调（驱动波形） |
| F3 | 本地转写 | ✅ | faster-whisper medium，GPU 0.18s / CPU 1.6s，CUDA 预检防挂起，断网可用 |
| F4 | LLM 保守纠错 | ✅ | DeepSeek：去语气词、标点断句、谐音术语修正（配森→Python 实测通过）、三档强度 |
| F5 | 文本注入 | ✅ | safe_inject：剪贴板全格式备份恢复、UIPI 预检、冲突检测、句柄所有权严格管理 |
| F6 | 悬浮窗状态反馈 | ✅ | 录音（音频波）→ 转写中 → 润色中 → 预览（草稿平滑过渡） |
| F7 | 词库 | ✅ | 词库.txt 热词拼入 ASR initial_prompt 与 LLM 提示词（数据定界防注入） |
| F8 | 配置 | ✅ | config.json 全项可配，原子写入，含脱敏示例 config.example.json |

### 2.2 P1 增强功能 —— 部分完成 🟡

| 编号 | 需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| F9 | 润色强度切换 | 🟡 部分 | 三档强度可配置（`refine.level`），但**仅改配置重启生效，无运行时热切换**（热键/悬浮窗） |
| F10 | 云端 ASR 兜底 | ✅ | `cloud_asr.py` + `asr.engine="cloud"` 配置切换（OpenAI 兼容端点） |
| F11 | 注入失败降级 | ✅ | 失败时文字保留剪贴板 + 悬浮窗提示；已区分"注入成功但剪贴板未恢复"语义 |
| F12 | 历史记录 | ❌ 未实现 | 无输入历史/统计功能 |

### 2.3 P2 路线图功能 —— 未开始（按 PRD 规划）

自学习纠错引擎、语音指令（换行/发送/删除）、翻译模式、人设/风格模板、方言多语种、macOS 版——均未实现，属后续路线。

### 2.4 完成度小结

| 优先级 | 完成度 |
| --- | --- |
| P0（演示闭环） | **100%** |
| P1（增强） | 约 60%（F10/F11 完成，F9 半完成，F12 未做） |
| P2（路线图） | 0%（全部留待后续） |

**总体评价**：作为一个黑客松/个人工具定位的 MVP，核心价值闭环完整，超出"最小可用"水平；后续价值集中在 P1 补全与差异化功能（自学习引擎）。

---

## 3. 代码与架构质量

### 3.1 优点
- **模块划分清晰**：热键/录音/ASR/润色/注入/UI/VAD 各司其职，单文件职责单一；main.py 用状态机编排，未变成大泥球
- **线程安全设计**：音频回调线程只写共享变量，UI 操作全部路由到 Tk 主线程轮询；悬浮窗先映射再读尺寸（修复错位根因）
- **健壮性**：CUDA 预检、GPU→CPU 双重重试、LLM 超时重试、剪贴板句柄所有权严格管理（多轮审查打磨）
- **安全**：词库内容定界防注入（`<words_data>`）、命令派发去掉 shell 包装（`claude.cmd`）、密钥配置不入库

### 3.2 专业审查记录（open-code-review，8 轮）

| 指标 | 初扫 | 终态 |
| --- | --- | --- |
| 总评论 | 43 | 28~38（波动） |
| critical | 1 | **0** |
| high | 11 | **0** |
| security | 2 | 0~1（medium，已缓解） |

残留 16 medium + 22 low 均为可维护性打磨项：死代码/未用变量、docstring 与实现不一致、魔法数字、理论性线程安全、日志粒度等，**无功能性缺陷**。

### 3.3 技术债清单
| 项 | 影响 | 建议 |
| --- | --- | --- |
| `injector.py` 为备用模块，main 已用 safe_inject | 冗余，两套注入逻辑 | ✅ 已删除（v5.9），注入统一走 safe_inject |
| 无自动化单元测试 | 回归靠手动 | 补 pytest（VAD/draft_smoother/refiner/config 可测性强） |
| 无 CI | 提交质量无门禁 | GitHub Actions：py_compile + pytest + doctor |
| 模型 1.5GB 不入库 | 新用户需手动下载 | 一键下载脚本（`download_model.py`）或发布 Release 附件 |
| 残留 medium/low 打磨项 | 低 | 按需清理，不必追求字面零 |

---

## 4. 文档质量 —— 优秀 ✅

| 文档 | 内容 | 状态 |
| --- | --- | --- |
| README.md（根目录） | 项目门面：功能、结构、快速开始、配置、使用、FAQ、更新记录 | ✅ GitHub 首页正常渲染 |
| 部署文档.md | 技术栈/原理、实现逻辑、环境要求、部署 8 步、运行、维护、FAQ | ✅ 322 行，命令实测可用 |
| PRD / SPEC | 需求决策、功能矩阵、验收标准、技术规格 | ✅ 完整 |
| 调研文档 | 竞品分析、开源项目、技术选型 | ✅ |
| OCR审查报告 | 8 轮审查与修复过程记录 | ✅ |

---

## 5. 测试与验证

| 类型 | 现状 |
| --- | --- |
| 注入回归测试 | 3 个跨进程脚本（tests/），半自动 |
| 手动验证 | toggle 状态机、真实按键模拟、ASR GPU 0.18s、RMS 链路、UI 截图、doctor 7 项自检 |
| 环境自检 | doctor.py（依赖/配置/麦克风/GPU）✅ 实测全绿 |
| **缺口** | 无自动化单测、无 CI、无端到端（E2E）自动化 |

**已知环境注意点**：在 Codex 终端这类带剪贴板桥接的环境里，注入可能受剪贴板外部干扰；在正常 Windows 桌面（记事本等）不受影响。

---

## 6. 风险与问题

| 风险 | 等级 | 说明与缓解 |
| --- | --- | --- |
| 模型下载依赖人工 | 中 | 部署文档已覆盖 ModelScope 命令；建议加一键下载脚本 |
| 注入方案依赖剪贴板+模拟按键 | 中 | 管理员窗口被 UIPI 拦截（已预检提示）；个别应用兼容性需实测 |
| 无 CI/单测 | 中 | 后期维护回归风险；建议 GitHub Actions + pytest |
| DeepSeek API 单点 | 低 | 已支持云端 ASR 兜底与润色降级（无 key 直接输出原始转写） |
| 单人项目可持续性 | 低 | 文档齐全、代码清晰，可交接 |

---

## 7. 客户端 GUI 界面规划（本次调研补充）

### 7.1 同类产品界面调研结论

| 产品 | 平台 | 界面组成 | 值得借鉴 |
| --- | --- | --- | --- |
| Typeless | macOS/Windows | 菜单栏图标 + 设置窗口（API/模型/麦克风/热键）+ 录音悬浮条 + 互动音效 | 设置窗口分栏；API Key 可清空 + Test/Save 按钮 |
| 智谱 AI 输入法 | Win/macOS | 托盘 + 设置（人设、快捷键、词典/热词）+ 历史记录 + 人设 Alt+数字 快捷切换 | 词典管理界面、人设列表、快捷键切换模式 |
| Wispr Flow | Win/macOS | 分类设置窗口（General/Microphone/Sound/Experimental）+ Flow Bar 悬浮条（右键菜单） | 设置分类页签；悬浮条右键快捷操作 |
| superwhisper | macOS | 菜单栏图标 + 分类设置（Advanced/Microphone/Sound）+ mini/regular 两种录音窗 + 自定义模式 | mini 录音窗与本项目胶囊形态一致；自定义 AI 指令模式 |
| FlashVoice | Windows | 主界面=历史记录管理 + 语音输入设置界面 + 本地模型管理界面 | 历史记录作为主界面；模型管理界面 |
| 微信桌面版 | Windows | 毛玻璃视觉（Fluent 风格） | 视觉质感参考 |

**共同规律**：① 菜单栏/托盘常驻入口；② 分类页签式设置窗口（通用/麦克风/模型/LLM/词库/人设）；③ 迷你录音窗或悬浮条；④ 历史记录界面；⑤ 深色/毛玻璃视觉；⑥ 声音反馈提示状态。

### 7.2 本项目 GUI 现状与缺口

**已有**：迷你波形胶囊悬浮窗（录音/转写/润色/预览四状态，不抢焦点）+ 右键退出。

**缺口**（对照调研）：

| 缺失项 | 现状 | 影响 |
| --- | --- | --- |
| 托盘/菜单栏图标 | 无常驻入口，只能靠悬浮窗右键退出 | 退出/开关/状态感知不便 |
| 设置窗口 | 全部配置靠手改 config.json | 普通用户门槛高，无 Test/Save 反馈 |
| 历史记录界面 | 无 | 无法回看/复用自己的输入 |
| 词库管理界面 | 手改 词库.txt | 热词维护不友好 |
| 人设/模式管理 | 无 | 风格化能力无法被用户使用 |
| 声音反馈 | 无 | 录音起止状态仅靠视觉 |

### 7.3 建议的 GUI 架构（按优先级）

**P0（建议立即排入短期计划）**
1. **托盘图标 + 右键菜单**：显示状态（空闲/录音中/处理中）、开始/停止录音、打开设置、退出——替代"只能右键悬浮窗退出"
2. **设置窗口**（tkinter 页签式，即改即存）：
   - 通用：热键、开机自启、语言、静音自动停止秒数
   - 麦克风：输入设备下拉、音量电平测试条
   - 模型：引擎（本地/云端）、模型路径、一键下载按钮（ModelScope）
   - LLM：启用开关、API Base URL / API Key（可清空）/ 模型 / 润色强度三档、**Test 与 Save 按钮**（参考 Typeless）
   - 词库：词条列表增删改（替代手改 词库.txt）
3. **历史记录窗口**：最近输入列表（时间 / 原始转写 / 最终结果），点击回看

**P1（中期）**
4. 人设/模式管理界面（与自学习引擎、风格化配套）
5. 悬浮条增强：右键快捷菜单（切换润色强度、打开历史）
6. 录音起止声音反馈（可关闭）

### 7.4 设计原则（综合调研结论）

- 深色为主、半透明/毛玻璃质感，与现有胶囊悬浮窗视觉统一（参考微信/智谱的质感路线）
- 设置即改即存（复用 config.py 的原子写入），LLM 配置提供 Test 按钮验证连通性
- 键盘优先：主要操作保留快捷键（热键录音、Alt+数字切换模式等）
- 保持"后台无感"：GUI 按需出现，不打断输入流程；录音时依然只显示小胶囊

---

## 8. 后续路线图

### 8.1 短期（1 周内，低成本高价值，建议优先）
1. **补 F9 运行时润色强度切换**（热键或悬浮窗按钮循环 conservative/light/polish）——P1 唯一半成品
2. **补 F12 输入历史记录**（内存队列 + 可选 JSON 落盘，悬浮窗快捷键回看）
3. **加 pytest 单元测试**（VAD 状态机、draft_smoother 帧序列、refiner prompt 组装、config 原子读写、hotkey 防抖逻辑）
4. **GitHub Actions CI**：push 时跑 `py_compile` + `pytest` + `doctor.py --ci`，质量门禁
5. **一键模型下载脚本** `download_model.py`（ModelScope，可选 small/medium）
6. **托盘图标 + 设置窗口 + 历史记录界面**（详见 §7.3 P0，与功能改进并行）
7. 清理技术债：`injector.py` 与旧注入测试已删除（v5.9）；残留 medium/low 中易修项（未用变量、docstring）待后续按需清理

### 8.2 中期（1~2 个月）
- **自学习纠错引擎**（PRD 差异化卖点）：纠错对沉淀 → 规则库 → 拼入提示词，越用越像你
- **语音指令**：换行 / 发送 / 删除 / 撤销（编辑指令层）
- **GUI 增强**：人设/模式管理界面、悬浮条右键快捷菜单、声音反馈（§7.3 P1）
- **打包 exe**（PyInstaller），发布 GitHub Release，真正"双击即用"

### 8.3 长期
- 翻译模式、人设/风格模板（对应 P2）
- macOS 版（`提示词.txt` 已有 Swift 方案设计：Fn 键、NSPanel 胶囊窗、IMK 注入）
- 移动端 / 跨平台（sherpa-onnx 方案可复用）

---

## 9. 独立复核补充（2026-08-03 · 二轮）

本节由第二轮独立复核（未先读本项目文档，直接审查代码后对照文档）补充，验证并扩展第 1~8 节结论。

### 9.1 实测验证（非纸面判断）

| 验证项 | 结果 |
| --- | --- |
| 14 个 Python 模块语法编译 | 全部通过（Python 3.13.3） |
| doctor.py 环境自检 | 7/7 全绿（依赖/配置/API Key/词库/麦克风/GPU） |
| 本地模型 GPU 加载 | small 1.8s / medium 3.4s（离线可用） |
| GPU 转写 6s 音频 | small 155ms / medium 369ms（与文档宣称 0.2~0.4s 相符） |
| 主程序启动 | 正常进入就绪态，无崩溃 |
| VAD / cloud_asr / asr 自检 | 全部通过，词库热词生效 |
| API Key 泄露面 | config.json 已 gitignore，Git 全历史无该 Key |
| 代码规模 | 14 模块 3,500 行（与第 1 节一致） |

### 9.2 复核发现的问题清单

| # | 问题 | 级别 | 状态 |
| --- | --- | --- | --- |
| 1 | 无自动化单测与 CI，回归靠手动 | 中 | 未处理（建议短期优先） |
| 2 | doctor 热键校验接受组合键，hotkey.py 仅支持单键（配置组合键会"自检通过但静默失效"） | 中 | 未处理（建议修复校验或实现组合键） |
| 3 | draft_smoother 自检在 GBK 控制台打印 ✓ 触发 UnicodeEncodeError | 低 | 未处理（v5 起该模块未接线，影响面更小） |
| 4 | DeepSeek Key 明文存 config.json，无环境变量支持 | 低 | 未处理（分享目录时需排除该文件） |
| 5 | config.json 使用绝对模型路径，移动项目即失效 | 低 | 未处理（建议启动时解析相对路径） |
| 6 | injector.py 与 safe_inject.py 双实现冗余 | 低 | ✅ 已处理（v5.9 删除 injector.py 与旧测试） |
| 7 | Overlay 忽略传入 root 自建第二个 Tk 根窗口 | 低 | 未处理（实测功能正常，Tcl 事件循环线程内共享） |
| 8 | 默认模型名 "small" 会尝试联网 HuggingFace（本机实测失败） | 中 | 已缓解（实际配置指向本地模型路径） |
| 9 | README 声明 Apache-2.0 但仓库无 LICENSE 文件 | 低 | ✅ 已处理（v5.9 新增 Apache-2.0 LICENSE） |
| 10 | PRD/SPEC 与实现存在文档漂移（按住说话/右 Ctrl/HF 下载/预览流程等） | 低 | 已处理（本次在 SPEC/PRD 追加变更记录并同步 README/部署文档） |
| 11 | 长录音全程音频驻留内存，时长线性增长 | 低 | 设计取舍（保证整段转写质量） |

### 9.3 v5 变更记录（本次已实现）

- **v5.1 说话才显示声波**：录音中仅当 VAD 判定"正在说话"时绘制 5 根波形，静音/未说话时只显示「🎤 录音中…」提示，直观反馈"我正在说话"（ui.py 新增 `set_speaking`，main.py 每 100ms 上报 VAD 状态）。
- **v5.2 移除转写预览**：结束录音后不再在浮窗展示转写文本，润色完成直接注入输入框；状态提示（转写中/润色中/注入中）保留（main.py 移除 PREVIEW 帧循环，draft_smoother 未接线、保留备用）。
- **v5.3 修复呼吸点颜色溢出**：修复转写/润色状态呼吸点动画在亮度系数超过 1.0 时 RGB 通道溢出 255、生成非法颜色名（如 `#102a511`）导致 Tkinter 回调异常的问题；亮度系数与颜色通道均钳制到合法范围（ui.py `_tick_dot`）。
- **v5.4 注入成功不再报剪贴板错误**：safe_inject 将"注入成功但剪贴板恢复未完成"从失败改为成功返回；main.py 不再弹 ERROR，仅控制台 `[剪贴板]` 日志提示（原因多为其他程序在注入瞬间修改剪贴板，如剪贴板管理器/桥接环境）。
- **v5.5 响应提速**：① `Recorder.stop()` 不再等待草稿线程（原 join 最长阻塞 2s，实测导致"按完成键后 1~2s 才转写"），改为 epoch 守卫的即时音频收集；② 新增逐帧更新"快速说话指示"（RMS 双阈值滞回，实测 1 帧约 10ms 响应，替代 VAD 120/200ms 去抖）；波形平滑系数 0.3→0.5，移除每 2 秒草稿重绘重置。实测：stop 阻塞 0ms，声波开关各 1 帧。
- **v5.6 有声就有波形**：声波触发阈值从约 -35dB 降至约 -44dB（开启 0.02 / 关闭 0.012，滞回防抖），轻声、气声、"嗯/啊"等语气词只要出声即触发波形；显示端增加平方根压缩，实测 -41dB 轻声音量显示高度约 17%（原几乎不可见）。实测：-41dB 触发、-43dB 触发、接近静音熄灭，全部通过。
- **v5.7 音乐播放器式频谱波形**：声波从 5 根音量条升级为 15 根频带波形——`Recorder._compute_bands` 对每帧音频做 FFT（对数频带 80Hz~8kHz，形状按帧峰值归一化），UI 以"频带形状 × RMS 响度增益 × 逐条平滑 × 包络"绘制；采集块细化 20ms/帧（blocksize=320），胶囊 180×56。实测：200Hz/1kHz/3kHz 峰值分别落在 #2/#8/#11 频带，音调变化驱动形状变化。
- **v5.8 静音静止、说话律动**：录音中不再显示任何文字，只显示声波；未说话时绘制低矮静止波形（中间略高的固定包络），说话时波形明显律动——响度增益 ×1.2、平滑系数 0.6、"峰值保持-回落"（每帧衰减 12%）与逐条相位摆动。实测：静音两帧画布完全一致（静止）、说话帧间变化（律动）、输入停止后峰值逐步回落。
- **v5.9 项目整理与文档完善**：删除冗余 `injector.py` 与旧注入测试（`run_inject_test.py`）、历史 Agent 工作区/结果（`tasks/` `results/` 由编排工具按需重建）、过期截图与全部 `__pycache__`；新增 Apache-2.0 `LICENSE`；README/部署文档/SPEC/审核报告同步；§9.2 问题 #6/#9 关闭。
- **v5.11 阿里代码审查与修复**：open-code-review 第 9~11 轮（DeepSeek 评审，34/35/40 条评论），7 个 high 全部修复：refiner 非字符串内容、ASR 换模型并发竞态、剪贴板分配失败数据丢失、词库重试重复/写入崩溃、快速双击 recorder 竞态、测试残留误读；另修复热键生命周期、配置容错、VAD 锁外计算、错误脱敏等 20+ 项 medium。终扫无 high，残留 medium/low 为打磨项（详见 OCR审查报告.md）。
- **文档融合**：README（核心能力/使用说明/FAQ/更新记录）、部署文档（状态机/模块职责/FAQ/变更记录）、SPEC/PRD（追加实现变更记录）已同步。

### 9.4 复核结论

- 与原报告一致：P0 100%、P1 约 60%、critical/high 清零、文档完备、适合演示与个人日常使用；
- 新增两项交互变更（v5）按用户反馈完成，浮窗从"展示转写内容"回归"纯状态提示"定位；
- 下一步仍建议按第 8 节短期清单推进：pytest + CI、组合键校验修复、一键模型下载脚本、托盘与设置窗口。

---

## 10. 总体结论

**评价**：这个项目已达到"黑客松/个人工具"定位的**优秀 MVP 水准**——

- ✅ **核心闭环 100% 完成**且经过 3 轮真实用户反馈迭代（交互、稳定性、UI 全部按反馈优化）
- ✅ **代码质量经过专业工具 8 轮审查**，严重问题清零，架构清晰、健壮性到位
- ✅ **文档完备**（README/部署/PRD/SPEC/审查报告），部署命令实测可用
- 🟡 P1 完成约 60%（缺 F9 运行时切换、F12 历史记录）
- 🟡 工程化有提升空间（无单测/CI/一键脚本）

**建议**：当前版本适合直接用于比赛演示或个人日常使用。若要继续投入，按 §8.1 的 5 项短期任务推进（约一周工作量），即可把项目从"优秀 MVP"提升到"工程化产品雏形"；其中"自学习纠错引擎"是区别于市面竞品的关键差异化，建议作为中期核心。

---

*报告由 AI 编排层基于项目实际代码、文档、审查记录与运行验证生成。*
