# 修复 SPEC：深度审核报告（第二轮）2026-08-04 遗留问题

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

> **Goal:** 修复《深度审核报告_第二轮_2026-08-04.md》的 N-M1/N-m1~N-m6 六项属实问题，以及复核时新发现的 6 个问题；每项修复后先验证再打勾，全部完成后清理无用文件并推送 GitHub。
>
> **Architecture:** 以最小侵入方式加固既有代码：轮询与数值读取统一走锁内局部变量 + 类型钳制；热键/注入/doctor/设置路径补齐防御；隐私密钥清出 config.json。每项均有针对性测试与验证命令，验证通过才把 `[ ]` 改为 `[x]`。
>
> **Tech Stack:** Python 3.12/3.13、pytest 9、Windows-only（pynput/sounddevice/Tk/ctypes）、faster-whisper 1.2.x。

---

## 0. 基线

- 版本基线：v5.16（本地 main = `5e67c56`，远端已同步）
- 当前测试：`python -m pytest voice_ime/tests -q --ignore=voice_ime/tests/run_safe_inject_test.py --ignore=voice_ime/tests/target_window.py` → 77 passed
- 前置结论（上一轮复核）：第二轮报告 N-m1~N-m6 属实；N-M1 的“崩溃”机制不可达，但“锁外两次读 recorder”仍应加固；另发现 6 个新问题（数值配置容错、焦点失败丢文本、历史中途开启不生效、单字符热键大小写、doctor 仅校验主热键、M1 报告行号有误）。

**统一验证命令：**

```powershell
python -m pytest voice_ime/tests -q --ignore=voice_ime/tests/run_safe_inject_test.py --ignore=voice_ime/tests/target_window.py
python -m compileall -q voice_ime
```

**不纳入本次修复：**

- N-m5 的“平台侧轮换 key”：需要用户在 DeepSeek 控制台操作，代码侧只负责清空 config.json 明文并提示改用 `DEEPSEEK_API_KEY`。
- 审核报告文档（含第二轮报告）：保留为审计历史，不删除。

---

## A. 核心修复（N-M1 + N-m1~N-m5）

### A1. N-M1 — `_poll_recording` 锁内取局部 recorder

- 修改：[main.py](voice_ime/main.py) `_poll_recording`：锁内同时读 `state` 与 `recorder` 到局部变量，锁外全程用局部变量（与 `_poll_level` 对齐）
- 测试：[test_main.py](voice_ime/tests/test_main.py) 新增“recorder 为 None 时轮询不崩溃且继续调度”用例
- 验证：`python -m pytest voice_ime/tests/test_main.py -q` → 通过

- [x] A1 已修复并验证（`test_main.py` 8 passed：recorder=None 轮询不崩溃且继续调度）

### A2. N-m1 — hotkey worker 内 stop 不再阻塞投递

- 修改：[hotkey.py](voice_ime/hotkey.py) `stop()` worker 分支：`put` 改 `put_nowait` + `except queue.Full: pass`（代数已递增，哨兵丢失也会退出）
- 测试：[test_hotkey.py](voice_ime/tests/test_hotkey.py) 新增“队列满时 worker 内 stop 不死锁”用例（预填满队列再触发）
- 验证：`python -m pytest voice_ime/tests/test_hotkey.py -q` → 通过

- [x] A2 已修复并验证（`test_hotkey.py` 8 passed：队列满时 worker 内 stop 不死锁）

### A3. N-m2 — inject 恢复延迟类型/范围钳制 + docstring 修正

- 修改：
  - [safe_inject.py](voice_ime/safe_inject.py)：`inject` 入口把 `restore_delay_sec` 钳制到 `[0.05, 5.0]`，非数字回退 `DEFAULT_RESTORE_DELAY_SEC`；第 516 行 docstring 的 “~25ms” 改为 “~200ms（可配置）”
- 测试：[test_safe_inject.py](voice_ime/tests/test_safe_inject.py) 新增“非数字/超范围延迟回退默认”用例
- 验证：`python -m pytest voice_ime/tests/test_safe_inject.py -q` → 通过

- [x] A3 已修复并验证（`test_safe_inject.py` 6 passed：非数字回退 0.2s、999 钳制 5s）

### A4. N-m3 — doctor 的 API Key 检查支持环境变量

- 修改：[doctor.py](voice_ime/doctor.py) `_check_api_key` 改用 `resolve_keys(data)`，未配置但 `DEEPSEEK_API_KEY` 存在时判通过，提示文案注明“配置文件或环境变量”
- 测试：[test_doctor.py](voice_ime/tests/test_doctor.py) 新增“env key 判通过”“config key 判通过”用例
- 验证：`python -m pytest voice_ime/tests/test_doctor.py -q` → 通过

- [x] A4 已修复并验证（`test_doctor.py` 5 passed：env/config/missing 三态）

### A5. N-m4 — 默认配置与 config.example.json 对齐

- 修改：[config.py](voice_ime/config.py)：`DEFAULT_CONFIG["asr"]["model"] = "models/faster-whisper-medium"`、`"language": None`（与 example 一致，避免照 example 拷贝得到非默认行为）
- 测试：[test_config.py](voice_ime/tests/test_config.py) 更新默认值断言
- 验证：`python -m pytest voice_ime/tests/test_config.py -q` → 通过

- [x] A5 已修复并验证（`test_config.py` 10 passed：默认模型/语言与 example 对齐）

### A6. N-m5 — config.json 清空明文 API Key

- 修改：[config.json](voice_ime/config.json)（gitignored，仅本地）：`refine.api_key` 置空；运行提示用户改用 `DEEPSEEK_API_KEY` 环境变量并轮换旧 key
- 验证：`Select-String -Pattern 'sk-' voice_ime/config.json` 无命中

- [x] A6 已修复并验证（config.json JSON 有效，`refine.api_key` 为空，无 `sk-` 残留）

---

## B. 体验与健壮性（N-m6 + 新发现 6 项）

### B1. N-m6 — 自动结束录音时有提示

- 修改：[main.py](voice_ime/main.py)：`_finish_recording(reason=None)`；max_duration 触发传 `reason="max_duration"`，静音超时传 `reason="silence"`，对应 toast 提示“已达最大录音时长/静音超时，已自动结束”
- 测试：[test_main.py](voice_ime/tests/test_main.py) 新增“max_duration reason 触发 toast”“reason 不改变状态机”用例
- 验证：`python -m pytest voice_ime/tests/test_main.py -q` → 通过

- [x] B1 已修复并验证（`test_main.py` 9 passed：max_duration toast + 状态机正常）

### B2. 新增问题1 — 录音/UI 数值配置容错

- 修改：
  - [config.py](voice_ime/config.py)：新增 `clamp_number(value, default, lo=None, hi=None)` 工具
  - [main.py](voice_ime/main.py)：`_poll_recording` 的 `auto_stop_silence_sec`/`max_duration_sec`、`_show_toast` 的 `preview_sec`、启动时 `max_chars` 全部走钳制（非数字回退默认，不再杀死轮询/启动）
  - [ui.py](voice_ime/ui.py)：`Overlay.__init__` 对 `max_chars` 自身容错
- 测试：[test_main.py](voice_ime/tests/test_main.py)、[test_ui.py](voice_ime/tests/test_ui.py) 新增“非数字配置不崩溃且轮询继续”用例
- 验证：`python -m pytest voice_ime/tests/test_main.py voice_ime/tests/test_ui.py -q` → 通过

- [x] B2 已修复并验证（`test_main/test_ui` 17 passed：非数字配置不再杀死轮询/启动）

### B3. 新增问题2 — 焦点/UIPI 中止时文本复制到剪贴板

- 修改：[safe_inject.py](voice_ime/safe_inject.py)：新增 `_put_text_to_clipboard(text)`；UIPI 拦截与焦点切换分支先尝试把文本写入剪贴板，消息提示“已复制到剪贴板，请手动粘贴”（失败则提示无法写入）
- 测试：[test_safe_inject.py](voice_ime/tests/test_safe_inject.py) 更新焦点中止用例 + 新增 UIPI 中止用例
- 验证：`python -m pytest voice_ime/tests/test_safe_inject.py -q` → 通过

- [x] B3 已修复并验证（`test_safe_inject.py` 7 passed：焦点/UIPI 中止均降级写剪贴板）

### B4. 新增问题3 — 设置页中途开启历史立即生效

- 修改：[settings_ui.py](voice_ime/settings_ui.py)：`save()` 保存后若 `history.enabled` 且 `app.history is None`，立即创建 `HistoryStore`（复用配置的 max_entries）
- 测试：[test_settings_ui.py](voice_ime/tests/test_settings_ui.py) 新增“中途开启后 app.history 立即创建”用例
- 验证：`python -m pytest voice_ime/tests/test_settings_ui.py -q` → 通过

- [x] B4 已修复并验证（`test_settings_ui.py` 5 passed：中途开启历史立即挂载 store）

### B5. 新增问题4 — 单字符热键大小写兼容

- 修改：[hotkey.py](voice_ime/hotkey.py)：`HotkeyListener` 记录单字符键的小写集合；`_on_press` 中按键事件 char 小写化后匹配（Caps Lock 开启时按 A 也能触发配置为 a 的热键）
- 测试：[test_hotkey.py](voice_ime/tests/test_hotkey.py) 新增“配置 a 时大写 A 事件可触发”用例
- 验证：`python -m pytest voice_ime/tests/test_hotkey.py -q` → 通过

- [x] B5 已修复并验证（`test_hotkey.py` 9 passed：大写 A 事件可触发单字符热键）

### B6. 新增问题5 — doctor 校验全部三个热键

- 修改：[doctor.py](voice_ime/doctor.py)：`_check_config` 除主热键外，同时校验 `settings_hotkey`/`refine_cycle_hotkey`（空串视为禁用，合法；非法/组合键报错）
- 测试：[test_doctor.py](voice_ime/tests/test_doctor.py) 新增“非法设置热键被拒”“空设置热键放行”用例
- 验证：`python -m pytest voice_ime/tests/test_doctor.py -q` → 通过

- [x] B6 已修复并验证（`test_doctor.py` 新增 2 用例；受改文件合计 45 passed）

---

## C. 文档、全量验证、清理与推送

### C1. README v5.17 变更记录

- 修改：[README.md](README.md)：追加 v5.17 条目，汇总本轮修复
- 验证：README 可正常渲染（走查）

- [x] C1 已修复并验证（README v5.17 条目已追加，走查通过）

### C2. 全量验证

- 运行：
  - `python -m pytest voice_ime/tests -q --ignore=...` → 全部通过（预计 90+）
  - `python -m compileall -q voice_ime` → 无错误
  - `python -c "import main, hotkey, safe_inject, doctor, settings_ui"` → 正常
  - `cd voice_ime; python doctor.py` → 可运行（API Key 项按预期提示环境变量）

- [x] C2 已修复并验证（92 passed、compileall OK、imports OK、doctor 7/7 且识别 DEEPSEEK_API_KEY）

### C3. 清理无用文件

- 删除：`voice_ime/__pycache__`、`voice_ime/tests/__pycache__`、`voice_ime/.pytest_cache`（生成物）；如有 `result.txt`/`target_ready.txt`/`history.json` 一并删除
- 保留：`深度审核报告_2026-08-04.md`、`深度审核报告_第二轮_2026-08-04.md`（审计历史）
- 验证：`git status --short` 仅剩预期变更与未跟踪报告

- [x] C3 已修复并验证（缓存已删；无临时/隐私产物；两份审核报告按约定保留）

### C4. 提交并推送 GitHub

- 分阶段提交（A/B/C 各一次），推送到 `origin/main`
- 验证：`git push origin main` 成功，GitHub API 确认远端 HEAD = 本地 HEAD

- [ ] C4 已修复并验证

---

## 完成标准

1. 本文件所有复选框均为 `[x]` 且附验证结果
2. 全量测试通过、compileall 通过
3. `config.json` 无明文 `sk-` 密钥
4. 无新增未跟踪的临时/隐私文件
5. 变更已推送至 GitHub（origin/main）
